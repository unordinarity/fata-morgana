{
  "name": "fata-morgana",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "develop": "nx develop @fata-morgana/client",
    "lint": "nx run-many --target=lint",
    "test": "nx run-many --target=test",
    "build": "nx build @fata-morgana/client",
    "serve": "nx serve @fata-morgana/client",
    "clean": "nx run-many --target=clean"
  },
  "devDependencies": {
    "lerna": "^6.5.1",
    "nx": "15.7.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18.0.0",
    "yarn": ">=1.0.0 <2.0.0"
  }
}
